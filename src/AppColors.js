export const primaryColor = '#6666ea';
export const secondColor = '#ffa500';
export const lightColor = '#e4e4e4';
export const darkColor = '#22222a';

const customStyles = {
  primaryColor,
  secondColor,
  lightColor,
  darkColor
};

export default customStyles;
